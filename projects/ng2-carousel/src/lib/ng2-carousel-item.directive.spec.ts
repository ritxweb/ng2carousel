import { Ng2CarouselItemDirective } from './ng2-carousel-item.directive';

describe('Ng2CarouselItemDirective', () => {
  it('should create an instance', () => {
    const directive = new Ng2CarouselItemDirective();
    expect(directive).toBeTruthy();
  });
});
